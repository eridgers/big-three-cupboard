extends ../layout.pug

block content
  h1 #{title}
    .ui.tiny.image.right.floated
      img(src=brand.logo, alt="#{brand.name}")
  p BELOW ITEMS WILL BE DELETED BY CASCADE
    form(id="delete-form" action=brand.url+"/delete" method="POST")
      button.ui.button.red(type="submit") YES DELETE THEM ALL
      a.ui.button.green(href="/brands") Never mind
  p
  .ui.divided.relaxed.items
    each item in items
      .item
        .ui.small.image
          img(src=item.image, alt="#{item.name}")
        .middle.aligned.content
          .header #{item.name}
          .meta
            span.price $#{item.cost} |
            span.weight #{item.weight} grams
          .description #{item.description}
          .extra
            a.ui.right.floated.primary.button(href=item.url) View Details
              i.right.chevron.icon
            a.ui.right.floated.button.inverted.secondary(href=item.category.url) View all #{item.category.name}
            .ui.label Lightweight!